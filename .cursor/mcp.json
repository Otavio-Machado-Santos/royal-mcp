{
  "mcpServers": {
    "royal": {
      "type": "stdio",
      "command": "${workspaceFolder}${/}target${/}release${/}royal-mcp",
      "args": [],
      "env": {
        "ROYAL_MCP_CONFIG": "${workspaceFolder}${/}config.toml"
      }
    }
  }
}
